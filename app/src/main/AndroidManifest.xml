<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RelaytiveLayout"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config"
        >
        <activity
            android:name=".User.Product.ProductDetailActivity" >
        </activity>
        <activity
            android:name=".User.Product.ProductListActivity"
            android:launchMode="standard"
            android:label="Danh sách sản phẩm"
            >
        </activity>
        <activity android:name=".Admin.AdminActivity" android:label="Quản trị dự án"></activity>
        <activity android:name=".Admin.Product.CreateProductActivity" android:label="Tạo sản phẩm"></activity>
        <activity android:name=".Admin.Product.ProductTable" android:label="Danh sách sản phẩm"></activity>
        <activity android:name=".User.Blogs.BlogList" android:label="Danh sách bài viết"></activity>
        <activity android:name=".User.Blogs.EditPostActivity" android:label="Danh sách bài viết"></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>